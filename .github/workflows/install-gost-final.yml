name: Install GOST

on:
  workflow_dispatch:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Install GOST on Germany server
        uses: appleboy/ssh-action@v1
        with:
          host: 178.105.61.169
          username: root
          password: "hTuRdTssfTTu"
          port: 22
          script: |
            cd /root
            wget https://github.com/go-gost/gost/releases/download/v3.2.5/gost_3.2.5_linux_amd64.tar.gz
            tar -xzf gost_3.2.5_linux_amd64.tar.gz
            chmod +x gost
            mv gost /usr/local/bin/
            rm gost_3.2.5_linux_amd64.tar.gz
            gost -V
