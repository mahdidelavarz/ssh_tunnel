name: Check Germany

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check GOST on Germany
        uses: appleboy/ssh-action@v1
        with:
          host: 178.105.61.169
          username: root
          password: "hTuRdTssfTTu"
          port: 22
          script: |
            ps aux | grep gost
            netstat -tlnp | grep 443
